<div data-controller="delivery" class="w-screen h-screen flex text-blue-bici-dark bg-blue-50">
  <%= render 'shared/sidebar' %>
  <div class="w-full" id="delivery_form">
    <%= render(Delivery::AppFormComponent.new(delivery: @delivery, city: @city, user: @user)) %>
  </div>
</div>
